"use client";

import styled from "styled-components";

const MainHeader = styled.header`
  display: flex;
`;

const Setting = () => {
  return (
    <MainHeader>
      <></>
    </MainHeader>
  );
};

export default Setting;
